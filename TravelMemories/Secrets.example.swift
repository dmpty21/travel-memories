import Foundation

// Copy this file into TravelMemories/TravelMemories/Secrets.swift (already gitignored)
// and fill in your real key. That folder is auto-compiled by Xcode, so the real
// file must live there — this example file stays one level up, out of the build.
//
// Get a free Pexels API key at https://www.pexels.com/api/

enum Secrets {
    static let pexelsAPIKey = "YOUR_PEXELS_API_KEY"
}
